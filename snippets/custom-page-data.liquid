<script>
    const pageData = {
        "title": "{{ page.title }}",
        "handle": "{{ page.handle }}",
        "content": "{{ page.content}}",
        "url": "{{ page.url}}",
        "metafields": {
            "brand": "{{ page.metafields.custom.brand}}"
        }
    }
</script>
